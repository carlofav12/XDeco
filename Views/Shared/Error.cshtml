@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Se ha producido un error al procesar su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID de solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de desarrollo</h3>
<p>
    Cambiar a<strong>Desarrollo</strong> mostrará información más detallada sobre el error que se ha producido.
</p>
<p>
    <strong>El entorno de desarrollo no debe estar habilitado para las aplicaciones implementadas.</strong>
    Puede dar lugar a la visualización de información confidencial de excepciones a los usuarios finales.
    Para la depuración local, habilite la opción <strong>Desarrollo</strong> entorno mediante la configuración de <strong>ASPNETCORE_ENVIRONMENT</strong> variable de entorno a <strong>Desarrollo</strong>
    y reiniciar la aplicación.
</p>
